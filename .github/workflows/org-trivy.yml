name: org-trivy

on:
  workflow_dispatch:
  schedule:
    - cron: "23 8 1 * *"  # monthly (minute-safe policy)

permissions: read-all

concurrency:
  group: org-trivy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  trivy:
    uses: gr8monk3ys/org-workflows/.github/workflows/reusable-trivy.yml@2659a9a32c4e6e3858f69a2f502210941a886d5a
