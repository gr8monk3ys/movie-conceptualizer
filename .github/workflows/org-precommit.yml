name: org-precommit

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions: read-all

concurrency:
  group: org-precommit-${{ github.ref }}
  cancel-in-progress: true

jobs:
  precommit:
    uses: gr8monk3ys/github/.github/workflows/reusable-precommit.yml@4a306a94d72d10b8d7835d697521a469fc6deab1
